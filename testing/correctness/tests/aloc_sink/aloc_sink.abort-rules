("# At the moment, there's a Wallaroo bug for 2 rollbacks in a row, so don't do that.")
("# So, possible failures are at least 2 IDs apart.")
("# We make them 3 apart to make certain there is at least one successful commit in between failures that also writes > 0 bytes.")
("local-txn",  3, False, False, False)
("local-txn",  6, False, True , False)
("local-txn",  9, False, False, True )
("local-txn", 12, True , False, False)
("local-txn", 15, True , True , False)
("local-txn", 18, True , False, True )
