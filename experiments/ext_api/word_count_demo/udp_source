#!/bin/bash

# change pwd to this script's folder
cd "$(dirname "${BASH_SOURCE[0]}")"

if [ ! -d _env ]; then
  make _env
fi
. _env/bin/activate

export PYTHONPATH=../../../machida/lib:$PWD:$PYTHONPATH
python udp_listener.py --udp-host 0.0.0.0 --udp-port 30000 --text-stream 127.0.0.1:7100
