#!/bin/bash

# change pwd to this script's folder
cd "$(dirname "${BASH_SOURCE[0]}")"

if [ ! -d _env ]; then
  make _env
fi
. _env/bin/activate

export PYTHONPATH=../../../machida/lib:$PWD:$PYTHONPATH
python kafka_producer.py --topic counts --count-stream 127.0.0.1:7200
